#!/bin/sh
set -eu

if command -v brain-validate >/dev/null 2>&1; then
	brain-validate .
elif [ -x .venv/Scripts/brain-validate.exe ]; then
	.venv/Scripts/brain-validate.exe .
elif [ -x .venv/bin/brain-validate ]; then
	.venv/bin/brain-validate .
else
	printf '%s\n' 'brain-validate is not installed; install the project with the Python command for your platform from README.md.' >&2
	exit 1
fi
